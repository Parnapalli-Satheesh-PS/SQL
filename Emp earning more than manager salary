We will use self join by joining emp_id and manager_id columns.

select e.emp_id as emp_id, e.salary as emp_salary, m.id as manager_id, m.salary as manager_salary
from emp e
join emp m
on e.manager_id = m.emp_id
where e.salary > m.salary;


Table
Input Columns
emp_id
emp name
salary
manager id

Output columns
emp id
emp name
salary
manager id
